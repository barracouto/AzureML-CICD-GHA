$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
command: python train.py --output ${{outputs.model_dir}}
code: .
environment: azureml:iris-env:1
compute: azureml:${{env.AML_COMPUTE}}
experiment_name: iris-train
description: Train iris model and write model.joblib to named output

identity:
  type: user_identity

outputs:
  model_dir:
    type: uri_folder
