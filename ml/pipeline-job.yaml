$schema: https://azuremlschemas.azureedge.net/latest/commandJob.schema.json
code: .
command: >
  python train.py --output ${{outputs.model_dir}}
environment: azureml:iris-env:1   # <— use the environment you registered in GHA
compute: azureml:cpu-cluster       # will be overridden by --set compute in CI
experiment_name: iris-train
description: Train iris model and store model.joblib in outputs

outputs:
  model_dir:
    type: uri_folder
    mode: upload
